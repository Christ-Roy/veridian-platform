# Handoff — 2026-04-05 après-midi

> **Objectif de ce doc** : permettre à Claude de reprendre le travail en autonomie pendant l'absence de Robert, puis à Robert de décharger le contexte et reprendre là où on s'est arrêté.
>
> **Lire ce doc en entier avant de toucher à quoi que ce soit.** Il contient le contexte complet du refactor SIREN-centric qui vient d'être livré, les tests restants, les pistes pour tester en autonomie via browser automation, et le plan pour les features admin (Hub + prospection).

---

## 1. État actuel — ce qui vient d'être livré

### 1.1 Branche git

Branche active : **`staging`** (repo `prospection/`, local uniquement, **jamais pushée**).

Commits récents sur `staging` :
```
4a9bdff refactor: full SIREN-centric migration — results/domain → entreprises/siren  ← AUJOURD'HUI
68091bd Merge branch 'feat/workspaces-multi-user' into staging
abfd1bb feat: SIREN-centric refactor Phase 3 — entreprises table live
0bb0fbf test: add /invite/[token] page coverage to admin-routes.test.ts
8673cbb feat: workspaces multi-user + admin API + SIREN refactor foundation
```

**Ne pas push vers origin/staging sans accord explicite de Robert.** La convention Veridian c'est "jamais push sans le dire".

### 1.2 DB staging

**Container** : `compose-bypass-bluetooth-feed-tbayqr-prospection-db-1` sur le dev server (via Tailscale `100.92.215.42`).
**Accès** : `ssh dev-pub "docker exec -it <container> psql -U postgres -d prospection"`

**Tables clés** :

| Table | Rôle | Statut post-refactor |
|---|---|---|
| `entreprises` | Source de vérité, 996 657 SIREN, 95 cols, 23 indexes (3 composites) | **Primary** — toutes les queries passent par là |
| `outreach` | PK `(siren, tenant_id)`, workspace_id, statut funnel | Siren-centric ✓ |
| `call_log` | Appels Telnyx, FK siren → entreprises | Siren-centric ✓ |
| `followups` | Rappels programmés, FK siren | Siren-centric ✓ |
| `claude_activity` | Activité Claude (notes, analyses, drafts), FK siren | Siren-centric ✓ |
| `outreach_emails` | Emails envoyés, FK siren | Siren-centric ✓ |
| `lead_segments` | Segments manuels (add to segment), PK `(siren, segment, tenant_id)` | Siren-centric ✓ |
| `workspaces` | Sous-découpage tenant, admin/member roles | Actif |
| `workspace_members` | Liaison workspace ↔ user | Actif |
| `magic_links` | Invitations internes prospection | Actif |
| `segment_catalog` | Métadonnées des 31 VIEWs segments (v_s01→v_s30 + DIAMOND/GOLD) | Actif |
| **Legacy (présents, non utilisés par le code Prisma)** | | |
| `results` | Ancienne table domain-centric, 438K rows, 219 cols | **À DROP après 2 semaines de prod stable** |
| `email_verification`, `phone_verification` | Vérification email/phone legacy | À drop |
| `pj_leads` | Pages Jaunes scraping | À drop |
| `ovh_monthly_destinations` | Compteur OVH téléphonie | Utilisée par `server-call/route.ts` via raw SQL (pas via Prisma), laisser pour l'instant |

**Migrations SQL appliquées** (dans `prospection/dashboard/scripts/`) :
- `2026-04-04_add-workspaces.sql` — tables workspaces/members/magic_links
- `2026-04-04_add-entreprises-table.sql` — **obsolète, ne pas ré-appliquer**, remplacée par :
- `migration_pg_entreprises.sql` (de `tmp/prestaging-v36/`) — vraie table entreprises + indexes
- `2026-04-05_create-segments-views.sql` — 30 VIEWs segments + catalog
- `2026-04-05_rename-domain-to-siren.sql` — rename domain→siren sur les 5 tables métier + cleanup demo rows

### 1.3 Tests passants actuellement

**Vitest integration** (contre vraie DB staging) :
```bash
cd ~/Bureau/veridian-platform/prospection/dashboard
DATABASE_URL='postgresql://postgres:prospection-staging-2026@127.0.0.1:15432/prospection?connection_limit=5' \
SUPABASE_URL='...' SUPABASE_SERVICE_ROLE_KEY='...' NEXT_PUBLIC_SUPABASE_URL='...' \
NEXT_PUBLIC_SUPABASE_ANON_KEY='...' APP_URL='http://localhost:3000' \
npx vitest run e2e/integration/
```

Résultat attendu : **44/44 passent** (6 tenant-isolation + 10 workspace-isolation + 28 admin-routes). Durée ~35s.

**Playwright API tests** (contre dev server via Tailscale) :
```bash
BASE_URL=http://100.92.215.42:3000 CI=true PROSPECTION_URL=http://100.92.215.42:3000 \
HUB_URL=https://saas-hub.staging.veridian.site TENANT_API_SECRET=staging-prospection-secret-2026 \
SUPABASE_URL='...' SUPABASE_ANON_KEY='...' SUPABASE_SERVICE_ROLE_KEY='...' \
npx playwright test e2e/api-siren.spec.ts e2e/regression.spec.ts --reporter=list
```

Résultat attendu : **24/24 passent** (7 api-siren + 17 regression). Durée ~16s.

Les **credentials env** sont dans `~/credentials/.all-creds.env` (variables `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`). Le `TENANT_API_SECRET` est `staging-prospection-secret-2026` (staging) — dispo aussi dans le container prospection via `docker exec ... printenv | grep TENANT`.

**Helper scripts** déjà en place :
- `scripts/test-admin-routes.ts` — test CLI standalone des routes admin (17 assertions)
- `scripts/test-entreprises-routes.ts` — test CLI des nouvelles routes entreprises (26 assertions)
- `scripts/seed-staging-demo.ts` — seed 3 workspaces + 3 commerciaux fictifs

### 1.4 Dev server (Next dev) qui tourne sur Tailscale

**URL** : `http://100.92.215.42:3000`
**Process** : `next dev -H 0.0.0.0 -p 3000` tournant sur `ubuntu@dev-pub`, workdir `~/prospection-dev/`.
**Lancé via** : `setsid nohup npm run dev` (détaché, survit à la déconnexion SSH).
**Logs** : `ssh dev-pub "tail -50 /tmp/prospection-dev.log"`

**Watchdog** : `/tmp/next-watchdog.sh` sur dev-pub qui check `/api/health` toutes les 60s et restart si crash.

**Watcher rsync** (à relancer en local quand on modifie des fichiers pour qu'ils soient pushés auto) :
```bash
cd ~/Bureau/veridian-platform/prospection && nohup ./scripts/dev-sync.sh --watch > /tmp/dev-sync.log 2>&1 &
```

**Attention** : `node_modules` et `.prisma/client` sont **excluded** du rsync. Quand on modifie `schema.prisma`, il faut **régénérer manuellement** sur le dev server :
```bash
ssh dev-pub "cd ~/prospection-dev && npx prisma generate"
```
Puis restart next dev pour que le cache `.next` soit repris :
```bash
ssh dev-pub "ps -ef | grep 'prospection-dev.*next dev' | grep -v grep | awk '{print \$2}' | xargs -r kill"
ssh dev-pub "cd ~/prospection-dev && rm -rf .next && setsid nohup npm run dev -- -H 0.0.0.0 -p 3000 </dev/null >/tmp/prospection-dev.log 2>&1 & disown"
```

### 1.5 Auto-login (pour tester en browser)

Robert n'a **pas de password local** sur Supabase staging. Pour se logger dans le dashboard prospection, deux options :

**Option A — Token auto-login via API provision** (recommandé pour tests Claude) :
```bash
curl -s -X POST "http://100.92.215.42:3000/api/tenants/provision" \
  -H "Authorization: Bearer staging-prospection-secret-2026" \
  -H "Content-Type: application/json" \
  -d '{"email":"robert@veridian.site","name":"Robert","plan":"enterprise"}'
```
Réponse :
```json
{
  "tenant_id": "robert@veridian.site",
  "login_url": "http://100.92.215.42:3000/api/auth/token?t=<random>",
  ...
}
```
Ouvrir le `login_url` dans le browser pose directement le cookie sur le domaine prospection. Plan `enterprise` = pas de paywall.

**Option B — Magic link Supabase** (pour le Hub staging) :
```bash
curl -s -X POST "https://saas-api.staging.veridian.site/auth/v1/admin/generate_link" \
  -H "apikey: <SUPABASE_SERVICE_ROLE_KEY>" \
  -H "Authorization: Bearer <SUPABASE_SERVICE_ROLE_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"type":"magiclink","email":"robert@veridian.site"}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['action_link'])"
```

---

## 2. Ce qui reste à tester — roadmap priorité haute

### 2.1 🔴 CRITIQUE — Tester l'UI dashboard avec un vrai browser

Les **44 tests integration + 24 Playwright API passent**, mais **aucun test browser ne valide l'UI rendu** après le refactor radical. Ce sont les vrais trous :

**Hypothèses à valider** :
1. Les pages `/prospects`, `/pipeline`, `/historique`, `/segments`, `/settings` se chargent sans crash
2. Les tableaux affichent les colonnes correctement (le champ `domain` contient maintenant un SIREN 9 chiffres au lieu d'un nom de domaine web)
3. La fiche prospect `/leads/[domain]` marche quand `[domain]` est un SIREN
4. Les filtres (secteur, NAF, département, prospect_score) fonctionnent
5. La pagination retourne les bonnes données
6. L'écran de login redirige correctement
7. Le pipeline kanban ne crash pas avec 0 outreach (cleanup du rename migration)

**Fichiers UI qui référencent `lead.domain`** (à vérifier visuellement après load) :
```
src/components/dashboard/leads-table.tsx         ← tableau principal
src/components/dashboard/lead-sheet.tsx          ← fiche latérale
src/components/dashboard/lead-sheet/lead-header.tsx
src/components/dashboard/lead-sheet/sections.tsx
src/components/dashboard/lead-sheet/auto-save-notes.tsx
src/components/dashboard/segment-page.tsx
src/components/dashboard/segment-table.tsx
src/components/dashboard/prospect-page.tsx
src/components/dashboard/outreach-form.tsx
src/components/dashboard/pipeline-board.tsx
src/components/dashboard/calendar-dialog.tsx
src/app/historique/page.tsx
```

**Comment tester (plusieurs pistes)** :

#### Piste A — Claude in Chrome (browser MCP)
Il y a un MCP `mcp__claude-in-chrome__*` disponible (voir system prompt). Permet de naviguer, screenshot, read_page_text. Étapes :
```
1. mcp__claude-in-chrome__tabs_context_mcp  (voir tabs existants)
2. mcp__claude-in-chrome__tabs_create_mcp   (ouvrir nouveau tab)
3. mcp__claude-in-chrome__navigate  (aller sur login_url obtenu via /api/tenants/provision)
4. mcp__claude-in-chrome__read_page  (lire le contenu)
5. Screenshot pour debug visuel
```

Avant d'utiliser ces tools, **les charger via ToolSearch** :
```
ToolSearch query="select:mcp__claude-in-chrome__tabs_context_mcp"
ToolSearch query="select:mcp__claude-in-chrome__navigate,mcp__claude-in-chrome__read_page,mcp__claude-in-chrome__tabs_create_mcp"
```

#### Piste B — Playwright tests browser (préféré pour CI)
Créer un nouveau spec `e2e/ui-siren-smoke.spec.ts` qui :
1. Obtient un token auto-login via POST `/api/tenants/provision` en beforeAll
2. Ouvre `context.request.get(loginUrl)` pour poser le cookie, extrait les cookies, les injecte dans `context.addCookies()`
3. Navigue vers `/prospects`, `/pipeline`, `/historique`, `/segments`, `/settings`
4. Attend que le contenu se charge (pas de spinner infini)
5. Vérifie `page.locator('table, [role="table"], [data-testid="leads-list"]')` visible
6. Screenshot en cas d'échec (Playwright le fait par défaut)

**Modèle concret à copier** :
```ts
import { test, expect, request as pwRequest } from "@playwright/test";

const PROSPECTION_URL = process.env.PROSPECTION_URL || "http://100.92.215.42:3000";
const TENANT_API_SECRET = process.env.TENANT_API_SECRET || "staging-prospection-secret-2026";

test.beforeAll(async ({ browser }) => {
  // noop — we use per-test context
});

async function loginAs(email: string, browser: import("@playwright/test").Browser) {
  const api = await pwRequest.newContext();
  const provRes = await api.post(`${PROSPECTION_URL}/api/tenants/provision`, {
    headers: {
      Authorization: `Bearer ${TENANT_API_SECRET}`,
      "Content-Type": "application/json",
    },
    data: { email, name: email.split("@")[0], plan: "enterprise" },
  });
  const prov = await provRes.json();
  const tokenUrl = prov.login_url as string;

  // Follow the token endpoint in a context to get the cookie
  const context = await browser.newContext();
  const page = await context.newPage();
  await page.goto(tokenUrl);
  await page.waitForURL(/\/prospects|\/dashboard|\/$/, { timeout: 15000 });
  return { context, page };
}

test("/prospects loads without crashing", async ({ browser }) => {
  const { page } = await loginAs("robert@veridian.site", browser);
  await page.goto(`${PROSPECTION_URL}/prospects`);
  // Wait for a visible table or card container
  const table = page.locator('table, [data-testid="leads-list"]').first();
  await expect(table).toBeVisible({ timeout: 15000 });
  // Ensure no JS console error
  const errors: string[] = [];
  page.on("pageerror", (e) => errors.push(e.message));
  await page.waitForTimeout(1500);
  expect(errors).toHaveLength(0);
});

// Répéter pour /pipeline, /historique, /segments, /settings
```

#### Piste C — Test manuel via script tsx headless
Le plus rapide pour valider quelques URLs clés. Script `scripts/test-ui-pages.ts` :
```ts
import { chromium } from "playwright";

const PROSPECTION_URL = "http://100.92.215.42:3000";
const TOKEN = "..."; // fetch via provision

const PAGES = ["/prospects", "/pipeline", "/historique", "/segments", "/settings"];

async function main() {
  const browser = await chromium.launch();
  const context = await browser.newContext();
  const page = await context.newPage();
  // Auto-login
  await page.goto(`${PROSPECTION_URL}/api/auth/token?t=${TOKEN}`);
  await page.waitForURL(/\/prospects|\/dashboard/, { timeout: 15000 });

  for (const path of PAGES) {
    const errors: string[] = [];
    page.on("pageerror", (e) => errors.push(`${path}: ${e.message}`));
    try {
      await page.goto(`${PROSPECTION_URL}${path}`, { waitUntil: "networkidle", timeout: 20000 });
      console.log(`✓ ${path} loaded`);
    } catch (e) {
      console.log(`✗ ${path} FAILED: ${e}`);
    }
    if (errors.length) console.log(`  JS errors:`, errors);
  }
  await browser.close();
}
main();
```

### 2.2 🟡 Tester les endpoints avec auth complète

Les tests actuels vérifient les routes admin avec auth Supabase. Mais les routes métier (`/api/prospects`, `/api/leads/[domain]`, `/api/pipeline`, `/api/stats`) **n'ont pas de tests end-to-end AVEC une vraie session authentifiée**. Juste les tests "401 sans cookie".

**Créer** `scripts/test-dashboard-api.ts` qui :
1. Obtient un access_token via `/api/tenants/provision` puis `/api/auth/token`
2. Fait un `GET /api/prospects` avec le cookie → attend 200 + `{ data: [...], total: > 0 }`
3. Récupère un SIREN du premier résultat
4. Fait un `GET /api/leads/{siren}` → attend 200 + detail
5. Fait un `GET /api/stats` → attend 200 + `{ total: > 0, with_phone: > 0 }`
6. Fait un `GET /api/pipeline` → attend 200 + objet (vide OK)
7. Fait un `POST /api/outreach/[siren]` pour créer un outreach
8. Re-fait un `GET /api/pipeline` → attend la nouvelle row
9. `DELETE` ou update status pour nettoyer

### 2.3 🟡 Tests DB profondeur

Quelques queries à passer pour valider l'intégrité après rename :
```sql
-- Aucune row métier ne pointe sur un siren qui n'existe pas dans entreprises
SELECT COUNT(*) FROM outreach o LEFT JOIN entreprises e ON o.siren = e.siren WHERE e.siren IS NULL;
SELECT COUNT(*) FROM call_log c LEFT JOIN entreprises e ON c.siren = e.siren WHERE c.siren IS NOT NULL AND e.siren IS NULL;
SELECT COUNT(*) FROM followups f LEFT JOIN entreprises e ON f.siren = e.siren WHERE e.siren IS NULL;
SELECT COUNT(*) FROM claude_activity ca LEFT JOIN entreprises e ON ca.siren = e.siren WHERE e.siren IS NULL;
-- Toutes les VIEWs segments retournent des rows
SELECT segment_id, volume FROM segment_catalog ORDER BY volume DESC;
-- Les 3 indexes composites critiques sont actifs (EXPLAIN)
EXPLAIN SELECT * FROM entreprises WHERE is_registrar = false AND best_phone_e164 IS NOT NULL AND web_domain IS NULL ORDER BY prospect_score DESC LIMIT 50;
-- Attendu : "Index Scan using idx_ent_segment_sans_site"
```

### 2.4 🟢 Tests de régression visuelle (quand UI stable)

Une fois l'UI stable, ajouter des tests Playwright avec snapshots :
```ts
await expect(page).toHaveScreenshot("prospects-page.png", { maxDiffPixels: 100 });
```

---

## 3. Features à implémenter — Espace admin (Hub + prospection)

### 3.1 Contexte

Robert veut un **espace admin** dans les deux apps :
- **Hub** (saas-hub) — gérer tous les tenants de l'instance SaaS (liste, impersonation, grant plan, delete)
- **Prospection** — gérer les workspaces et commerciaux d'un tenant (déjà existe en backend, manque l'UI)

**État actuel** :
- Hub : routes API admin existent (`/api/admin/list-tenants`, `/grant-plan`, `/impersonate`, `/delete-tenant`) — pas d'UI
- Prospection : routes API admin complètes (workspaces CRUD, members, invites, kpi) — pas d'UI

### 3.2 Hub admin UI (à créer)

**Fichiers à créer** :
```
app.veridian/Web-Dashboard/app/dashboard/admin/page.tsx          ← landing admin
app.veridian/Web-Dashboard/app/dashboard/admin/tenants/page.tsx  ← liste tenants (existe via API)
app.veridian/Web-Dashboard/app/dashboard/admin/layout.tsx        ← auth guard
```

**Auth admin** — DÉJÀ EN PLACE côté API :
- Fichier : `app.veridian/Web-Dashboard/app/api/admin/list-tenants/route.ts`
- Mécanisme : whitelist `ADMIN_EMAILS = ['brunon5robert@gmail.com']` + vérification via `supabase.auth.getUser()` sur le Bearer token
- OR fallback `x-admin-secret` header vs env var `ADMIN_SECRET`
- **À faire** : déplacer `ADMIN_EMAILS` dans une env var `ADMIN_EMAILS` (CSV) pour ajouter d'autres admins sans rebuild

**Fonctionnalités à exposer dans l'UI** :
1. **Liste tenants** : tableau avec email user, plan, date création, services activés, boutons "Impersonate" + "Grant plan" + "Delete"
2. **Impersonation** : POST /api/admin/impersonate → retourne un `access_token` qu'on pose dans le cookie, puis redirect vers /dashboard. Permet à l'admin de voir l'app comme si c'était un user donné
3. **Grant plan** : dropdown pour changer freemium/pro/enterprise sur un tenant
4. **Delete tenant** : confirmation modal + POST /api/admin/delete-tenant

### 3.3 Prospection admin UI (à créer)

**Fichiers à créer** :
```
prospection/dashboard/src/app/admin/layout.tsx           ← auth guard via requireAdmin()
prospection/dashboard/src/app/admin/page.tsx             ← landing (redirect vers /admin/workspaces)
prospection/dashboard/src/app/admin/workspaces/page.tsx  ← CRUD workspaces
prospection/dashboard/src/app/admin/members/page.tsx     ← liste + invite
prospection/dashboard/src/app/admin/kpi/page.tsx         ← dashboard KPI commerciaux
```

**Auth admin** — DÉJÀ EN PLACE côté API :
- Fichier : `src/lib/supabase/user-context.ts`, fonction `requireAdmin()`
- Mécanisme : `ctx.isAdmin === true` (tenant owner OU workspace_member.role = 'admin')
- Retourne 403 si pas admin

**Pages à créer** :

#### `/admin/workspaces/page.tsx`
- Fetch `GET /api/admin/workspaces` → tableau des workspaces avec `memberCount`
- Bouton "Créer workspace" → modal + POST `/api/admin/workspaces`
- Chaque row : "Renommer" (PATCH), "Supprimer" (DELETE, confirmation car réassigne rows vers default)
- Slider "leads_limit" pour régler le quota par workspace

#### `/admin/members/page.tsx`
- Fetch `GET /api/admin/members` → tableau flat avec userId, email, role, workspaces
- Bouton "Inviter" → modal avec email + workspace + role → POST `/api/admin/invites` → affiche le magic link URL (bouton "Copier")
- Pour chaque member : boutons "Changer rôle" (PATCH), "Retirer" (DELETE)
- Badge "Tenant owner" pour l'user qui a créé le tenant (non-supprimable)

#### `/admin/kpi/page.tsx`
- Fetch `GET /api/admin/kpi?from=ISO&to=ISO` → renvoie agrégats par workspace
- Affichage : tableau par workspace avec colonnes :
  - Nom workspace
  - Nb members
  - Outreach total + byStatus (a_contacter, appele, rdv, gagne, perdu, rappeler)
  - Calls total + total_duration
  - Followups total + pending
  - Taux conversion (won / total)
- Filtres dates (jour / semaine / mois / custom)
- Leaderboard commerciaux (regroupement par userId)

### 3.4 Navbar admin

**Prospection** : ajouter un menu "Admin" dans la navbar visible si `ctx.isAdmin === true`. Le lien pointe vers `/admin/workspaces`. Fichier navbar à trouver : probablement `src/components/dashboard/layout/app-nav.tsx` ou similaire (créé dans le commit workspace).

**Hub** : même chose, condition = email in ADMIN_EMAILS.

### 3.5 Décisions structurantes en attente

1. **Unifier l'auth admin Hub + prospection ?** Actuellement Hub utilise ADMIN_EMAILS whitelist, prospection utilise workspace_members.role. Ça diverge.
   - **Option A** : garder divergence, documenter. Hub admin = super-admin plateforme, prospection admin = admin du tenant
   - **Option B** : unifier en passant Hub sur `tenants.is_platform_admin BOOLEAN` colonne, propagé via JWT claim
   - **Reco** : Option A pour MVP (simple), Option B en v2

2. **Magic links prospection → Hub sync** : quand l'admin prospection invite un user, est-ce qu'il doit aussi être créé dans le Hub ?
   - **Actuellement** : non, l'invite crée juste une row `magic_links` dans prospection-db + un user Supabase via `admin.createUser()`. Le user peut ensuite se logger via Supabase sur n'importe quelle app Veridian.
   - **Problème** : le Hub ne "connaît" pas ce user comme tenant. Faut-il provisioner un tenant Hub automatiquement ? Probablement non — seul le tenant owner a besoin d'un tenant Hub, les sales sont des membres du tenant existant.
   - **Action** : documenter clairement que les invites créent des "seats" dans le tenant owner, pas des nouveaux tenants.

---

## 4. Décisions restantes de Robert (6 arbitrages data hub)

D'après le handoff de l'agent data hub (cf. `tmp/prestaging-v36/SUMMARY.md`), 6 questions attendent Robert :

1. **Schéma 95 colonnes** : OK ou élaguer ? → **acté implicitement** (schéma importé tel quel)
2. **Seuil is_registrar ≥20 domaines** : 330 SIREN flaggés → **acté** (filtré par défaut)
3. **Auto-entrepreneurs réintégrés 53K** : exposés par défaut ou filtrés ? → **actuellement exposés**, filtrable via `?include_auto=false`
4. **ca_suspect flag 7 SIREN** : exclus du scoring → **acté** (filtré via `DEFAULT_ENTREPRISES_WHERE`)
5. **Rebonds BODACC 29K** : dans entreprises ou table séparée ? → **NON TRANCHÉ**. Table `rebonds_pool` **non importée** en staging. Si Robert veut les exposer, il faut l'importer et créer un segment dédié.
6. **Prospect_score formule** : cible 1797 diamond / 36K gold ou plus large ? → **acté** (valeurs conservées)

**Action pour Claude autonome** : ne pas toucher aux décisions non tranchées (rebonds BODACC). Documenter et passer.

---

## 5. Pistes d'amélioration techniques (nice-to-have)

### 5.1 Drop des tables legacy

Après 2 semaines de stable prod, drop :
```sql
DROP TABLE IF EXISTS results CASCADE;
DROP TABLE IF EXISTS email_verification CASCADE;
DROP TABLE IF EXISTS phone_verification CASCADE;
DROP TABLE IF EXISTS pj_leads CASCADE;
-- Garder ovh_monthly_destinations (utilisée par server-call)
```

Créer un script `scripts/2026-04-19_drop-legacy-tables.sql` à exécuter manuellement.

### 5.2 Import des 18 segments bonus S31-S48

Dans `tmp/prestaging-v36/segment_catalog.csv` il y a 51 segments (S01-S51), mais seuls S01-S30 sont dans la fonction `step_segments` de `enrich_prestaging.py`. Les 21 autres (S31-S51) ont été créés manuellement par l'agent data hub pendant la nuit.

**Action** : récupérer leurs définitions en interrogeant directement la DuckDB de Robert (`/home/brunon5/open-data-hub/data/prestaging.duckdb`) :
```bash
duckdb /home/brunon5/open-data-hub/data/prestaging.duckdb \
  -c "SELECT segment_id, view_name, description FROM segment_catalog WHERE segment_id NOT LIKE 'S0%' AND segment_id NOT LIKE 'S10' ORDER BY segment_id"
```
Puis extraire leur definition SQL via `SHOW CREATE VIEW v_sXX_*` (pas dispo en DuckDB, mais on peut retrouver la définition dans `information_schema.views`).

### 5.3 Import des rebonds BODACC (29K SIREN)

Si Robert tranche pour option A/B sur les rebonds. Fichier : `tmp/prestaging-v36/rebonds_pool.parquet`.

### 5.4 UI : switcher workspace dans la navbar

Décidé dans la roadmap `roadmap/09-workspaces-multi-user.md` Phase 4. Non fait.
- Dropdown en haut à droite : "Workspace actif : Paris ▼"
- Si admin : inclut "Toute l'organisation" qui reset le cookie `active_workspace_id`
- Si member : liste seulement ses workspaces

### 5.5 Migration propre via Prisma Migrate

Actuellement : drift entre schéma Prisma et DB staging (legacy tables `results` etc. existent en DB mais pas dans schema.prisma). Impossible d'utiliser `prisma migrate dev`, on bricole avec des ALTER chirurgicaux.

**À faire un jour** : baseline proper via `prisma migrate resolve` + créer les futures migrations via `prisma migrate dev`.

### 5.6 Audit final du code

Grep pour vérifier qu'il ne reste plus rien qui parle à `results` :
```bash
cd ~/Bureau/veridian-platform/prospection/dashboard
grep -rn "FROM results\|from \"Result\"\|prisma\.result\.\|results\\.domain\|pj_leads\|email_verification\|phone_verification" src/ --include="*.ts" --include="*.tsx" | grep -v generated
```
**Attendu** : 0 occurrences. Si > 0, refactorer ou documenter pourquoi c'est gardé.

---

## 6. Fichiers clés par domaine

### 6.1 Schéma / DB
- `prospection/dashboard/prisma/schema.prisma` — source de vérité Prisma
- `prospection/dashboard/scripts/2026-04-05_rename-domain-to-siren.sql` — dernière migration
- `prospection/dashboard/scripts/2026-04-05_create-segments-views.sql` — 30 VIEWs
- `tmp/prestaging-v36/migration_pg_entreprises.sql` — schéma entreprises (94 cols + 23 indexes)
- `tmp/prestaging-v36/SUMMARY.md` — decisions data hub

### 6.2 Auth + tenant
- `prospection/dashboard/src/lib/supabase/user-context.ts` — `getUserContext`, `requireAdmin`, `requireUser`, `getWorkspaceScope`
- `prospection/dashboard/src/lib/supabase/api-auth.ts` — `requireAuth` (legacy)
- `prospection/dashboard/src/lib/supabase/tenant.ts` — `getTenantId`, `getTenantProspectLimit`
- `prospection/dashboard/src/lib/supabase/middleware.ts` — middleware session refresh
- `prospection/dashboard/src/app/api/auth/token/route.ts` — endpoint auto-login via token

### 6.3 Queries metier (tous refactorés SIREN-centric)
- `prospection/dashboard/src/lib/queries/shared.ts` — `buildLeadsSelect/From`, `COLUMN_MAP`, `DEFAULT_ENTREPRISES_WHERE`
- `prospection/dashboard/src/lib/queries/leads.ts` — `getLeads`, `getLeadDetail`, `getLeadsBySiren`
- `prospection/dashboard/src/lib/queries/prospects.ts` — `getProspects`, `getDomainCounts`, `getPresetCounts`
- `prospection/dashboard/src/lib/queries/stats.ts` — `getStats`
- `prospection/dashboard/src/lib/queries/pipeline.ts` — `getPipelineLeads`, `updateOutreach`, `recordVisit`
- `prospection/dashboard/src/lib/queries/segments.ts` — `getSegmentLeads`, `getSegmentCount`, `addToSegment`
- `prospection/dashboard/src/lib/queries/activity.ts` — claude_activity, followups, outreach_emails
- `prospection/dashboard/src/lib/segments.ts` — SEGMENTS_TREE, NAF_GROUPS, TECH_SCORE_SQL, COLDCALL_BASE_WHERE

### 6.4 Routes API
- **Admin API prospection** : `src/app/api/admin/*` (workspaces, members, invites, kpi)
- **Entreprises API** : `src/app/api/entreprises/*` (list, [siren], segments, segments/[id])
- **Legacy API (refactored)** : `src/app/api/prospects`, `src/app/api/leads/[domain]`, `src/app/api/pipeline`, `src/app/api/stats`
- **Phone routes** : `src/app/api/phone/*` (call-log, server-call, summarize-call, telnyx-webhook)
- **Hub admin API** : `app.veridian/Web-Dashboard/app/api/admin/*` (list-tenants, grant-plan, impersonate, delete-tenant)

### 6.5 Tests
- `e2e/integration/tenant-isolation.test.ts` — 6 tests, isolation multi-tenant
- `e2e/integration/workspace-isolation.test.ts` — 10 tests, isolation workspace
- `e2e/integration/admin-routes.test.ts` — 28 tests, routes admin + entreprises
- `e2e/api-siren.spec.ts` — 7 tests Playwright API smoke
- `e2e/regression.spec.ts` — 17 tests Playwright existants
- `e2e/saas-flow.spec.ts` — tests flow signup (à fixer : test 8 token auto-login)
- `e2e/browser-flow.spec.ts` — tests browser (cascading échec sur "hub login → dashboard" = bug Hub existant, pas notre faute)
- `e2e/existing-accounts.spec.ts` — tests existing accounts

### 6.6 Scripts utilitaires
- `scripts/dev-sync.sh` — rsync local → dev server (--watch mode)
- `scripts/seed-staging-demo.ts` — seed workspaces + commerciaux démo
- `scripts/test-admin-routes.ts` — 17 assertions CLI admin routes
- `scripts/test-entreprises-routes.ts` — 26 assertions CLI entreprises routes
- `scripts/test-invite-flow.ts` — test flow magic link

### 6.7 Documentation
- `docs/README.md` — index doc
- `docs/architecture/workspaces-and-admin.md` — feature workspaces
- `docs/architecture/auth-and-tenants.md` — auth Supabase
- `docs/architecture/database-schema.md` — schéma Prisma
- `roadmap/09-workspaces-multi-user.md` — roadmap workspace
- `tmp/SIREN-REFACTOR-AUDIT.md` — audit 891 lignes du refactor SIREN
- `tmp/SIREN-REFACTOR-GAPS.md` — gaps vs specs data hub
- `tmp/prestaging-v36/` — tout le livrable agent data hub

### 6.8 Memory projet (Claude Code)
Dans `~/.claude/projects/-home-brunon5-Bureau-veridian-platform/memory/` :
- `MEMORY.md` — index (toujours chargé dans le contexte)
- `feedback_agent_teams_default.md` — règle : TeamCreate dès 2 agents
- `project_siren_refactor_assets.md` — pointeurs vers assets tmp/
- `session_2026-04-03_env_fixes.md` — dernière session prod (à jour)
- Plein d'autres sessions...

---

## 7. Commandes rapides (copier-coller)

### Relancer les tests integration
```bash
cd ~/Bureau/veridian-platform/prospection/dashboard && \
DATABASE_URL='postgresql://postgres:prospection-staging-2026@127.0.0.1:15432/prospection?connection_limit=5' \
SUPABASE_URL='https://saas-api.staging.veridian.site' \
SUPABASE_SERVICE_ROLE_KEY="$(ssh dev-pub 'docker exec compose-bypass-bluetooth-feed-tbayqr-prospection-1 printenv SUPABASE_SERVICE_ROLE_KEY')" \
NEXT_PUBLIC_SUPABASE_URL='https://saas-api.staging.veridian.site' \
NEXT_PUBLIC_SUPABASE_ANON_KEY="$(ssh dev-pub 'docker exec compose-bypass-bluetooth-feed-tbayqr-prospection-1 printenv NEXT_PUBLIC_SUPABASE_ANON_KEY')" \
APP_URL='http://localhost:3000' \
npx vitest run e2e/integration/
```
(Note : passer par le tunnel SSH `ssh -f -N -L 15432:localhost:15432 dev-pub` avant, sinon localhost:15432 n'existe pas)

### Relancer les tests Playwright
```bash
cd ~/Bureau/veridian-platform/prospection/dashboard && \
BASE_URL=http://100.92.215.42:3000 CI=true \
PROSPECTION_URL=http://100.92.215.42:3000 \
HUB_URL=https://saas-hub.staging.veridian.site \
TENANT_API_SECRET=staging-prospection-secret-2026 \
SUPABASE_URL='https://saas-api.staging.veridian.site' \
NEXT_PUBLIC_SUPABASE_URL='https://saas-api.staging.veridian.site' \
SUPABASE_ANON_KEY="$(ssh dev-pub 'docker exec compose-bypass-bluetooth-feed-tbayqr-prospection-1 printenv NEXT_PUBLIC_SUPABASE_ANON_KEY')" \
NEXT_PUBLIC_SUPABASE_ANON_KEY="$(ssh dev-pub 'docker exec compose-bypass-bluetooth-feed-tbayqr-prospection-1 printenv NEXT_PUBLIC_SUPABASE_ANON_KEY')" \
SUPABASE_SERVICE_ROLE_KEY="$(ssh dev-pub 'docker exec compose-bypass-bluetooth-feed-tbayqr-prospection-1 printenv SUPABASE_SERVICE_ROLE_KEY')" \
npx playwright test e2e/api-siren.spec.ts e2e/regression.spec.ts
```

### Obtenir un login URL pour tester
```bash
curl -s -X POST "http://100.92.215.42:3000/api/tenants/provision" \
  -H "Authorization: Bearer staging-prospection-secret-2026" \
  -H "Content-Type: application/json" \
  -d '{"email":"robert@veridian.site","name":"Robert","plan":"enterprise"}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['login_url'])"
```

### TypeScript check
```bash
cd ~/Bureau/veridian-platform/prospection/dashboard && npx tsc --noEmit
```

### ESLint
```bash
cd ~/Bureau/veridian-platform/prospection/dashboard && npx eslint src/ --quiet
```

### Restart next dev sur dev server
```bash
ssh dev-pub "ps -ef | grep 'prospection-dev.*next dev' | grep -v grep | awk '{print \$2}' | xargs -r kill"
sleep 2
ssh dev-pub "cd ~/prospection-dev && rm -rf .next && setsid nohup npm run dev -- -H 0.0.0.0 -p 3000 </dev/null >/tmp/prospection-dev.log 2>&1 & disown"
sleep 15
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://100.92.215.42:3000/api/health
```

### Regenerer Prisma client sur dev server
```bash
ssh dev-pub "cd ~/prospection-dev && rm -rf node_modules/.prisma && npx prisma generate"
```

### Check DB staging
```bash
ssh dev-pub "docker exec compose-bypass-bluetooth-feed-tbayqr-prospection-db-1 psql -U postgres -d prospection -c 'SELECT COUNT(*) FROM entreprises;'"
ssh dev-pub "docker exec compose-bypass-bluetooth-feed-tbayqr-prospection-db-1 psql -U postgres -d prospection -c 'SELECT segment_id, volume FROM segment_catalog ORDER BY volume DESC;'"
```

---

## 8. Ordre de priorité recommandé pour Claude autonome

1. **Valider l'UI visuellement** (§2.1) via Playwright browser tests ou MCP chrome. Screen + check aucun crash JS sur les 5 pages principales.
2. **Tester les API métier avec auth** (§2.2) via script tsx qui obtient un token puis hit /api/prospects, /api/leads/[siren], etc.
3. **Audit final grep** (§5.6) pour confirmer qu'il ne reste plus aucune référence à `results` dans le code.
4. **Écrire un résumé** des bugs UI trouvés (s'il y en a) pour que Robert puisse décider quoi fixer au réveil.
5. **Créer les stubs des pages admin** (§3.2 et §3.3) — layouts + premier render qui fetch les data. Ne pas polir le CSS, juste prouver que ça charge.
6. **Commit chaque étape** sur `staging` avec message clair. **Ne pas pusher.**

**Si bloqué** : ne pas casser des choses pour débloquer. Écrire dans `tmp/HANDOFF-BLOCKERS-2026-04-05.md` ce qui bloque et pourquoi.

---

## 9. Credentials + accès

| Resource | Où trouver |
|---|---|
| Supabase staging keys | `ssh dev-pub "docker exec compose-bypass-bluetooth-feed-tbayqr-prospection-1 printenv \| grep SUPABASE"` |
| TENANT_API_SECRET staging | `staging-prospection-secret-2026` (literal) |
| DB password staging | `prospection-staging-2026` |
| Postgres port staging | `15432` (local seulement, via `ssh dev-pub` ou tunnel `-L 15432:localhost:15432`) |
| DuckDB data hub | `/home/brunon5/open-data-hub/data/prestaging.duckdb` (machine locale seulement) |
| Credentials globaux | `~/credentials/.all-creds.env` (machine locale Robert) |
| Admin emails Hub | Hardcoded : `brunon5robert@gmail.com` dans `list-tenants/route.ts` |

---

## 10. Ce que Claude NE doit PAS faire sans autorisation

1. **Push vers origin/staging ou origin/main** — jamais
2. **Toucher à la prod OVH** (port 80/443 publics, compose-index-*) — jamais
3. **DROP de tables en DB staging** — jamais (même les legacy)
4. **Modifier `~/credentials/.all-creds.env`** — jamais
5. **`rm -rf` sur quoi que ce soit en dehors de `/tmp/` ou `.next/`** — jamais
6. **`prisma migrate dev`** — le schéma a une drift avec la DB, ça casserait tout. Utiliser des ALTER SQL chirurgicaux.
7. **Toucher au code du Hub (app.veridian/Web-Dashboard)** — OK pour lecture, OK pour créer des **nouvelles** pages admin, **pas modifier** les routes existantes sans accord.
8. **Régénérer le parquet entreprises** — c'est fait côté data hub par un autre agent, on consomme son output via `tmp/prestaging-v36/entreprises.parquet`.

---

**Fin du handoff. Bonne autonomie.** 🤖
